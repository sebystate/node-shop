const config = {
  development: {
    //url to be used in link generation
    url: 'http://localhost:3000',
    //mongodb connection settings
    MONGODB_URI:
      'mongodb+srv://<USER>:<PASSWORD>@cluster0.zzijgaq.mongodb.net/<DATABASE>?retryWrites=true&w=majority',
    //server details
    server: {
      host: '127.0.0.1',
      port: '3000',
    },

    // Mail Server (SendGrid) details
    mail_server: {
        API_KEY: 'here_goes_the_mail_server_api_key',
        // verified address on SendGrid that you have to verify in case of SendGrid
        from_address: 'verifiedaccount@test.com'
    }
  },
};
module.exports = config;
